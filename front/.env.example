# Base de l'API. Vide = chemins relatifs /api (proxy Vite en dev, nginx en prod). RECOMMANDÉ.
VITE_API_BASE_URL=
# Cible du proxy /api en dev (npm run dev) :
VITE_API_PROXY_TARGET=http://localhost:8080
