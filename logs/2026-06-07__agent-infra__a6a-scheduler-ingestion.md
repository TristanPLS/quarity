# Log — agent-infra — 2026-06-07 — a6a-scheduler-ingestion

## 23:54 CEST — A6a : scheduler d'ingestion périodique (boucle tokio + service compose ingest-scheduler)

- **Agent / rôle** : agent-infra
- **Jalon / tâche** : Jalon 3 — backlog **A6a** (scission A6a/A6b actée : l'ORDONNANCEMENT de l'ingestion est livré ici ; le backfill S3 reste en **A6b**. La mise à jour de `docs/backlog.md` sera faite par l'orchestrateur APRÈS le commit B5 en attente — fichier volontairement non touché).
- **Contexte** : depuis la PR 15 l'ingestion OpenAQ est robuste (pagination, retry/backoff, unités strictes) mais reste un one-shot manuel ; la donnée doit vivre toute seule. Décisions actées : Option A (boucle tokio DANS le binaire, zéro nouvelle dépendance — tokio `full` déjà présent), `INGEST_INTERVAL_SECS` absente/0 = one-shot inchangé, > 0 = long-running ; nouveau service compose `ingest-scheduler` SANS profil, `restart: unless-stopped`, arrêt propre SIGTERM.
- **Actions** :
  - **`ingest.rs` — refactor sans changement de comportement** : le corps de `main()` est extrait tel quel en `async fn run_ingest() -> Result<()>` (mêmes messages, mêmes étapes, sortie anticipée à 0 mesure, pool Postgres créé À CHAQUE run pour éviter un pool stale ; `argv[1]` relu à chaque run, prioritaire sur `OPENAQ_LOCATION_ID`, donc appliqué à tous les ticks).
  - **`fn parse_interval_secs(raw: Option<&str>) -> Result<u64>`** : fonction PURE (valeur brute en paramètre, pas d'accès env — testable sans sérialisation). Absente, vide ou blanche → 0 (one-shot ; le cas « vide » est assimilé à « absente » car compose passe `""` quand la variable hôte n'est pas posée) ; entier ≥ 0 → n ; sinon **erreur explicite → exit 1** (une erreur de configuration doit être bruyante, comme le parse de `location_id`).
  - **`async fn run_scheduler(interval_secs: u64)`** : premier run IMMÉDIAT, puis `tokio::time::interval` + `MissedTickBehavior::Delay` (un run plus long que l'intervalle décale le tick suivant — PAS de rattrapage en rafale qui martèlerait OpenAQ ; documenté en commentaire). L'échec d'un run est loggué (`eprintln!` avec la chaîne anyhow `{e:#}` + « run en échec — nouvel essai au prochain tick ») et la boucle CONTINUE : clé API vide, org pas seedée, ClickHouse qui redémarre ne tuent JAMAIS le conteneur. Logs de tick horodatés UTC (numéro de tick + prochain réveil), style `println!` conservé (pas de tracing, hors scope).
  - **Arrêt propre** : `spawn_shutdown_listener()` (tâche dédiée + canal `tokio::sync::watch`) écoute Ctrl-C ET, en `cfg(unix)`, SIGTERM (`tokio::signal::unix` — ce que `docker stop` envoie ; compile aussi sous Windows via `cfg(not(unix))` = Ctrl-C seul). Signal pendant l'ATTENTE (`tokio::select!` tick/shutdown) → sortie propre exit 0 ; signal pendant un RUN → le canal watch le mémorise, le run se termine puis on sort (runs de quelques secondes ; si docker SIGKILL après sa grâce de ~10 s, l'idempotence ReplacingMergeTree/upserts rend l'interruption récupérable — documenté en commentaire).
  - **Tests unitaires `#[cfg(test)]` dans ingest.rs** : 5 tests purs sur `parse_interval_secs` (absente, vide/blanche, 0, positifs avec espaces, invalides `abc`/`-5`/`1.5`/`1h`/`10 20` → message citant la variable ET la valeur). Zéro réseau, zéro env globale, zéro service → CI-safe dans `cargo test --all`.
  - **`docker-compose.yml`** : nouveau service `ingest-scheduler` SANS profil (démarre avec `docker compose up -d`), `image: quarity-back:local`, `entrypoint: ["quarity-ingest"]`, `restart: unless-stopped`, mêmes variables que le service `ingest` PLUS `INGEST_INTERVAL_SECS: ${INGEST_INTERVAL_SECS:-3600}` et `OPENAQ_DAYS: ${INGEST_SCHEDULER_DAYS:-1}` (fenêtre courte : couvre le retard de publication OpenAQ, divise par 7 le bruit des MV rollup vs 7 j — piège §C.4 du schéma CH rappelé en commentaire YAML). `depends_on` : postgres + clickhouse `service_healthy`, back `service_started` (c'est le back qui applique les migrations PG ; sur stack vierge un tick trop précoce échoue proprement et est retenté — commenté). Le service `ingest` one-shot (profil `ingest`) est **byte-identique** ; seul l'en-tête du fichier mentionne le nouveau service.
  - **Cas piège `INGEST_INTERVAL_SECS=0` + `restart: unless-stopped`** (décision demandée) : choix = défaut compose `:-3600` jamais à 0 + INTERDICTION DOCUMENTÉE (commentaire YAML + .env.example) de poser 0 pour « neutraliser » le scheduler — avec 0 le binaire one-shot exit 0 et la politique de restart le relancerait en boucle serrée (spam OpenAQ). La bonne commande de suspension est `docker compose stop ingest-scheduler`. Pas de garde dans le binaire : « 0 = one-shot actuel inchangé » est une décision actée (et le mode 0 reste légitime pour `--profile ingest run --rm`).
  - **`.env.example`** : section « Scheduler d'ingestion (A6a) » — `INGEST_INTERVAL_SECS` (défaut 3600 ; 0/absente = one-shot pour les runs manuels `run --rm` ; avertissement boucle de restart ci-dessus), `INGEST_SCHEDULER_DAYS` (défaut 1, justification MV/retard de publication), rappel que `docker compose up -d` démarre désormais le scheduler et qu'une `OPENAQ_API_KEY` vide = ticks en échec loggés (pas de crash) jusqu'à la pose de la clé + recréation du service.
- **Fichiers touchés** :
  - `back/src/bin/ingest.rs` (modifié — refactor `run_ingest` + `parse_interval_secs` + `run_scheduler` + signaux + 5 tests unitaires)
  - `docker-compose.yml` (modifié — ajout du service `ingest-scheduler` + en-tête ; service `ingest` intact)
  - `.env.example` (modifié — section scheduler)
  - `logs/2026-06-07__agent-infra__a6a-scheduler-ingestion.md` (créé — ce log)
- **Résultat** : OK — les 4 vérifications imposées sont vertes, ingestion réelle re-validée en one-shot ET en mode scheduler.
- **Vérifs** :
  - `cargo fmt --all -- --check` OK ; `cargo clippy --all-targets -- -D warnings` OK (0 warning) ; `cargo check --all-targets` OK ; `cargo test --all -- --test-threads=1` (env stack locale posée) → **74 passed; 0 failed** (dont `cargo test --bin ingest` → 5/5 tests `parse_interval_secs`).
  - **Régression one-shot** (hôte, env localhost:55432/8123, clé réelle) : SANS `INGEST_INTERVAL_SECS` → « Station #4085 « NICE PROMENADE » (FR) — 4 capteur(s) … ✓ 220 mesures insérées … ✓ Station liée à l'org « agglo-riviera » … ✅ Terminé. » **exit 0** (sortie identique à avant, idempotente). `INGEST_INTERVAL_SECS=0` → identique, exit 0. `INGEST_INTERVAL_SECS=abc` → `Error: INGEST_INTERVAL_SECS invalide : « abc » n'est pas un nombre entier de secondes >= 0 (invalid digit found in string)` **exit 1**.
  - **Mode scheduler** (`INGEST_INTERVAL_SECS=20`, ~50 s) : 3 ticks complets observés, premier run immédiat, cadence exacte — « ⏰ tick #1 — 21:51:05 UTC … ✓ tick #1 : run terminé. … prochain réveil dans ~20 s (tick #2) », ticks #2 à 21:51:25 et #3 à 21:51:45.
  - **Résilience** (`CLICKHOUSE_PASSWORD` faux, 3 ticks) : « ⚠ tick #1 : run en échec — nouvel essai au prochain tick. Cause : statut HTTP ClickHouse: HTTP status client error (403 Forbidden) … » ×3, le process SURVIT et retente à chaque tick. Idem `OPENAQ_API_KEY` vide : 3 ticks en échec « 401 Unauthorized … (erreur client : échec immédiat, sans retry) », pas de crash.
  - `docker compose config -q` → 0 erreur ; rendu inspecté : `ingest-scheduler` avec `INGEST_INTERVAL_SECS: "3600"`, `OPENAQ_DAYS: "1"`, `restart: unless-stopped`, `depends_on` postgres/clickhouse `service_healthy` + back `service_started`, `entrypoint: [quarity-ingest]`.
  - Note Windows : l'arrêt SIGTERM est `cfg(unix)` (conteneur = cible réelle) ; sur l'hôte Windows les tests ci-dessus ont validé la boucle et la survie aux échecs, l'arrêt y passe par Ctrl-C.
- **Prochaine étape** : exécuter l'action git ci-dessous (après le commit B5 en attente), CI verte, squash merge ; l'orchestrateur mettra à jour `docs/backlog.md` (A6a livré, A6b backfill S3 restant) ; vérifier au prochain `docker compose up -d --build` que `quarity_ingest_scheduler` tourne et loggue ses ticks (`docker compose logs -f ingest-scheduler`).
- **Action Git suggérée à l'humain** :
  > ─────────────────────────────────────────────
  > Prochaine étape avant de continuer (à exécuter par toi) :
  > Branche cible : feature/infra-a6a-scheduler-ingestion   (créée depuis dev à jour — jamais master/dev en direct)
  > 1) git add back/src/bin/ingest.rs docker-compose.yml .env.example logs/2026-06-07__agent-infra__a6a-scheduler-ingestion.md
  > 2) git commit -m "feat(infra): A6a - scheduler d ingestion periodique (boucle tokio + service compose ingest-scheduler)"
  > 3) git push -u origin feature/infra-a6a-scheduler-ingestion
  > 4) gh pr create --base dev
  > Puis : attendre la CI verte, squash merge.
  > ─────────────────────────────────────────────
